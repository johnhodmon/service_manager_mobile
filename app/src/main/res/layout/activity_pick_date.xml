<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/green"
    android:paddingBottom="@dimen/activity_vertical_margin"

    tools:context="ie.hodmon.computing.hodmonpumpservices.PickDate">

    <CalendarView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/calendarView"
        android:layout_marginTop="16dp"
        android:layout_above="@+id/pickDateButton"
        android:selectedWeekBackgroundColor="@color/green"
        android:selectedDateVerticalBar="@color/green"
        android:layout_centerHorizontal="true"
        android:background="#ffffff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/pickDateButton"
        android:id="@+id/pickDateButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="confirmDate" />

</RelativeLayout>
